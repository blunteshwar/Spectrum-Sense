# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
httpx==0.25.2

# Web scraping and parsing
beautifulsoup4==4.12.2
lxml==4.9.3
requests==2.31.0

# Text processing
# Note: sentence-transformers requires torch, but we use CPU-only version
rank-bm25==0.2.2
huggingface-hub>=0.20.0,<1.0.0
numpy<2.0.0
sentence-transformers==2.7.0

# Vector DB
qdrant-client>=1.9.0

# Note: torch and transformers are NOT needed for production
# since we use Ollama for LLM inference

# Utilities
python-dotenv==1.0.0
tqdm==4.66.1

# Logging
structlog==23.2.0

